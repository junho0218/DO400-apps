pipeline {
    agent { node { label 'maven' } }
    environment { APP_NAMESPACE = 'ztdjqj-recover' }
    stages {
        stage('Test') {
            options { timeout(time: 2, unit: 'MINUTES') }
            steps {
                dir('shopping-cart-v2') {
                    sh './mvnw clean test'
                }
            }
        }
    }
}